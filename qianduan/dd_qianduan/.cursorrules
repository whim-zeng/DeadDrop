# DeadDrop 开发规范

## 最高优先级规则

你是一个前端工程师，正在实现一个已经完成设计的产品。
你的工作是把设计意图变成可运行的代码，而不是描述设计意图。

### 绝对禁止的行为：

1. **禁止用文字描述效果来替代实现效果。**
   - 错误：在页面上写"这里会有一个纸条飘动的动画"
   - 正确：用代码实现纸条飘动的动画
   - 错误：写一个placeholder说"老化效果将在此显示"
   - 正确：用CSS filter / Canvas 实现老化效果

2. **禁止使用注释占位符来代替真实实现。**
   - 错误：{/* TODO: 在这里实现墨水晕染 */}
   - 正确：用代码实现墨水晕染

3. **禁止在UI上显示任何技术说明、设计说明、规范说明。**
   用户看到的页面上不应出现任何解释"这个东西是什么"或"这个效果代表什么"的元文字。
   所有意图必须通过视觉、动画、交互本身来传达。

4. **禁止使用alert()、console.log展示状态。** 所有状态通过视觉变化表达。

5. **当你不确定一个视觉效果怎么实现时，用你能实现的最接近的方案去做，而不是用文字描述你做不到的方案。**

## 设计系统

### 色彩
- 背景主色: #0F0F1A（近乎纯黑的深蓝）
- 背景渐变: linear-gradient(135deg, #0F0F1A 0%, #1A1A2E 50%, #16213E 100%)
- 纸条底色: #F5F0E8（旧纸暖白）
- 纸条老化色: #E8DCC8（泛黄旧纸）
- 纸条濒死色: #D4C8A8（深度泛黄）
- 墨水色: #2C2C2C（干墨深灰，不是纯黑）
- 墨水褪色: #8B8589（褪色墨水灰紫）
- 情绪色-悲伤: #6B7AA1
- 情绪色-焦虑: #C4956A
- 情绪色-失落: #8B8589
- 情绪色-开心: #A8B5A2
- 情绪色-愤怒: #B85C5C
- 情绪色-平静: #7BA7A7
- 辅助文字: #6B7280
- 极淡提示文字: #4B5563

### 字体
- 纸条手写内容: font-family: 'LXGW WenKai', 'Caveat', cursive
- 界面正文: font-family: 'Noto Serif SC', 'Noto Serif', serif
- 辅助/极小文字: font-family: 'Noto Sans SC', 'Noto Sans', sans-serif

### 动效基准
- 所有页面过渡: duration 800ms, ease-out
- 元素出现: fade-in + translateY(20px→0), duration 600ms
- 纸条展开: duration 1200ms, cubic-bezier(0.16, 1, 0.3, 1)
- 光点呼吸: animation duration 3s-5s（每个光点随机不同周期）, ease-in-out, infinite
- 情绪词hover: scale(1.05), duration 200ms
- 页面间过渡: 先当前页fade-out 400ms, 黑屏停留600ms, 新页fade-in 400ms

### 间距与尺寸
- 纸条内边距: 32px
- 纸条最大宽度: 480px
- 纸条圆角: 2px（纸不是卡片，几乎不圆角）
- 元素间距基准: 24px
- 全屏页面内容居中，最大宽度 600px
- 地图全屏铺满viewport

### 绝对不使用
- 不使用 Material Design 组件
- 不使用任何明亮的背景色
- 不使用圆角大于 8px 的按钮或容器
- 不使用 box-shadow 做悬浮卡片效果
- 不使用红点、badge、通知气泡
- 不使用任何社交App的视觉范式（头像、聊天气泡、消息列表）
- 按钮文案不使用"提交""发送""确认"——使用"放下""捡起""轻轻放回"
